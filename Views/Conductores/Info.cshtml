@model SoteloProjectFramework.Models.tbChoferes

@{
    ViewBag.Title = "Info";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>@Model.Nombres @Model.PrimerApellido (@Model.tbDetallesChoferes.FirstOrDefault().Unidad)</h2>

<div class="container d-flex justify-content-around">

	<div>
		<div class="z-0 position-absolute  p-3 rounded bg-gradient-success text-light" style="width:12vw; height:10vw;">
			<div class="z-n1 position-absolute p-3 rounded bg-gradient-primary" style="width:12vw; height:9vw;">
				<a class="d-flex justify-content-center nav-link text-light" href="@Url.Action("Details", new { ChoferId = Model.ChoferId })">
					Datos Personales
				</a>
			</div>
		</div>
	</div>

	<div>
		<div class="z-0 position-absolute  p-3 rounded bg-gradient-success text-light" style="width:12vw; height:10vw;">
			<div class="z-n1 position-absolute p-3 rounded bg-gradient-primary" style="width:12vw; height:9vw;">
				<a class="d-flex justify-content-center nav-link text-light" href="@Url.Action("HistoryDriver", "Viajes", new { ChoferId = Model.ChoferId })">
					Historial
				</a>
			</div>
		</div>
	</div>

	<div>
		<div class="z-0 position-absolute  p-3 rounded bg-gradient-success text-light" style="width:12vw; height:10vw;">
			<div class="z-n1 position-absolute p-3 rounded bg-gradient-primary" style="width:12vw; height:9vw;">
				<a class="d-flex justify-content-center nav-link text-light" href="@Url.Action("Create2", "Viajes", new { ChoferId = Model.ChoferId })">
					Nuevo Registro
				</a>
			</div>
		</div>
	</div>



</div>

